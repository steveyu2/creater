const msg = {
  required: {
    useFilepath: 'file paths are required and .js files'
  },
  type: {
    textFile: 'files must be text files',
    format: 'format must be a function'
  }
};

module.exports = msg;
